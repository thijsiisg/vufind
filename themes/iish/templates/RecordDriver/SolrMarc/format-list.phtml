<? foreach ($this->driver->getFormats() as $format): ?>
  <span class="iconlabel <?=$this->record($this->driver)->getFormatClass($format)?>">
    <i class='fa fa-x fa-<?=$this->record($this->driver)->getFormatClass($format)?>'></i>
    <?=$this->transEsc($format)?>
  </span>
<? endforeach; ?>
